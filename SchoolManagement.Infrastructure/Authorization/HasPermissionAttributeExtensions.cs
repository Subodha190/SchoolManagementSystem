using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc.Filters;

namespace SchoolManagement.Infrastructure.Authorization
{
    // This file intentionally left minimal. The actual attribute is in the Application project
    // and maps to the dynamic policy provider via Policy = "Permission:{key}".
}
